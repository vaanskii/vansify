{
  "build": {
    "context": "backend",
    "args": {
      "DB_USER": "${{ DB_USER }}",
      "DB_PASS": "${{ DB_PASS }}",
      "DB_HOST": "${{ DB_HOST }}",
      "DB_PORT": "${{ DB_PORT }}",
      "DB_NAME": "${{ DB_NAME }}",
      "JWT_SECRET": "${{ JWT_SECRET }}",
      "SMTP_SERVER": "${{ SMTP_SERVER }}",
      "SMTP_PORT": "${{ SMTP_PORT }}",
      "SMTP_USER": "${{ SMTP_USER }}",
      "SMTP_PASS": "${{ SMTP_PASS }}"
    }
  }
}
